spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop

app:
  token-store: memory
  push: stomp
  storage:
    type: local
    local-dir: ./target/test-files
  pdf:
    font-path: ""
